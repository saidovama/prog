#include <stdio.h>

int main()
{  double R=5, r=4;
    double q=10;
    double l=q*(R*R)/(r*r);
    double p;// цена дом яиц
    scanf ("%lf", &p);
    double k;// цена инк яиц
    scanf ("%lf", &k);
    double i=(q*p)-(q*k);
    printf("%.2lf, %.2lf", p, i);
    return 0;
}
